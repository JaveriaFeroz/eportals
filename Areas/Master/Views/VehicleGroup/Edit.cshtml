@model ProcureToPay.Areas.Master.ViewModels.VehicleGroupViewModel

<div class="modal-body">
    <form id="vehicleGroupEditForm" method="post" asp-action="Edit" asp-controller="VehicleGroup" asp-area="Master"
          data-area="Master" data-controller="VehicleGroup">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="GroupId" />
        <div class="mb-3">
            <label for="GroupName" class="form-label">Group Name <span class="text-danger">*</span></label>
            <div class="input-group">
                <span class="input-group-text" style="background-color: #092963; color: white;"><i class="fas fa-dollar-sign"></i></span>
                <input id="GroupName" name="GroupName" class="form-control" value="@Model.GroupName" required />
            </div>
            <span asp-validation-for="GroupName" class="text-danger"></span>
        </div>


        <div class="mb-3">
            <label class="form-label"><i class="fas fa-power-off me-2"></i> Status</label>
            <div class="form-check form-switch">
                <input id="IsActive" name="IsActive" type="checkbox" class="form-check-input" value="true" @(Model.IsActive ? "checked" : "") />
                <label class="form-check-label" for="IsActive">Active</label>
                <input type="hidden" name="IsActive" value="false" />
            </div>
        </div>


        <div class="d-flex justify-content-end mt-4">
            <button type="button" class="btn btn-secondary me-2" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn" style="background-color: #092963; color: white;">
                <i class="fas fa-save me-1" style="color: white;"></i> Save Group
            </button>
        </div>

    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}