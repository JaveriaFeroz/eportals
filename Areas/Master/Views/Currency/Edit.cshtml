@model ProcureToPay.Areas.Master.ViewModels.CurrencyViewModel

<div class="modal-body">
    <form id="currencyEditForm" method="post" asp-action="Edit" asp-controller="Currency" asp-area="Master"
          data-area="Master" data-controller="Currency">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="CurrencyId" />
        <div class="mb-3">
            <label for="CurrencyName" class="form-label">Currency Name <span class="text-danger">*</span></label>
            <div class="input-group">
                <span class="input-group-text" style="background-color: #092963; color: white;"><i class="fas fa-dollar-sign"></i></span>
                <input id="CurrencyName" name="CurrencyName" class="form-control" value="@Model.CurrencyName" required />
            </div>
            <span asp-validation-for="CurrencyName" class="text-danger"></span>
        </div>


        <div class="mb-3">
            <label class="form-label"><i class="fas fa-power-off me-2"></i> Status</label>
            <div class="form-check form-switch">
                <input id="IsActive" name="IsActive" type="checkbox" class="form-check-input" value="true" @(Model.IsActive ? "checked" : "") />
                <label class="form-check-label" for="IsActive">Active</label>
                <input type="hidden" name="IsActive" value="false" />
            </div>
        </div>


        <div class="d-flex justify-content-end mt-4">
            <button type="button" class="btn btn-secondary me-2" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn" style="background-color: #092963; color: white;">
                <i class="fas fa-save me-1" style="color: white;"></i> Save Changes
            </button>
        </div>

    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}